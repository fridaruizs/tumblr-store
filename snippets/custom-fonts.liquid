{% doc %}
  Loads custom font files from the assets folder.

  @example
  {% render 'custom-fonts' %}
{% enddoc %}

@font-face {
  font-family: 'Bookman';
  src: url('{{ 'BOOKOSB.TTF' | asset_url }}') format('truetype');
  font-weight: 700;
  font-style: normal;
  font-display: swap;
}
